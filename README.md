# Add:
 - Reputation System
   /uprep {@user}
   /rep {@user}
   /toprep

# Dynamic voice channel:
 - Join -> Create and move member to temp channel -> Clean temp channel when no users in it.

# Scam message honeypot:
 - Same message different channel quickly -> Start warning.
version: '3.9'

services:
  techhelpbot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    working_dir: /app
    command: node src/index.js
